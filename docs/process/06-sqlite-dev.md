# 📝 Рецепт: Локальная разработка с SQLite

Этот документ описывает, как настроить и использовать локальную базу данных SQLite для разработки и тестирования.

---

## 🎯 Цель

Использование SQLite в качестве локальной базы данных позволяет:

- **Быстро стартовать:** Настройка окружения сводится к нескольким простым командам.
- **Работать безопасно:** Вы полностью изолированы от "боевой" (production) базы данных, что исключает риск случайной порчи реальных данных.
- **Разрабатывать оффлайн:** Не требуется подключение к облачной БД.
- **Иметь консистентные данные:** У каждого разработчика есть одинаковый набор начальных данных для тестирования.

---

## 🚀 Процесс разработки

Весь процесс работы с локальной базой данных сводится к трем основным командам.

### Шаг 1: Сброс (если нужно)

Если вы хотите начать с чистого листа и полностью удалить старую базу данных, выполните:

```bash
npm run db:reset-sqlite
```

**Что она делает?** Эта команда просто удаляет файл `dev.sqlite` и его временные файлы (`-wal`, `-shm`). Используйте её, когда ваши тестовые данные "замусорились" или вы хотите быть уверены в чистоте эксперимента.

### Шаг 2: Инициализация

Для создания и настройки базы данных с нуля, используйте:

```bash
npm run db:init:sqlite
```

**Что она делает?**

1.  Создает новый, пустой файл базы данных `dev.sqlite`.
2.  Запускает скрипт `scripts/init-sqlite.ts`, который считывает схему из `db/schema-portable.sql` и применяет её к созданной базе.
3.  **Важно:** Этот скрипт также наполняет (seeds) базу начальными данными (категории, тестовые товары), чтобы приложение сразу было работоспособным.

**Итог:** После выполнения шагов 1 и 2 у вас есть свежая база данных с актуальной схемой и тестовыми данными.

### Шаг 3: Запуск сервера разработки

Для повседневной разработки используйте команду:

```bash
npm run dev
```

Эта команда по умолчанию является псевдонимом для `npm run dev:sqlite` и запускает приложение в режиме разработки, подключенном к вашей локальной базе `dev.sqlite`.

---

## ⚠️ Важное ограничение среды

**Внимание:** В среде разработки Firebase Genkit Studio **отсутствует** консольная утилита `sqlite3` и возможность ее установки. Это означает, что вы не можете напрямую выполнять SQL-запросы к файлу `dev.sqlite` из командной строки терминала.

Взаимодействие с базой данных должно осуществляться **только через код**, используя библиотеку `better-sqlite3`, как это сделано в нашем адаптере `src/lib/db/adapters/sqlite-adapter.ts`.

---

## ⚙️ Как это работает (для любопытных)

- **`USE_SQLITE_DEV=true`**: Эта переменная окружения, установленная в скриптах `package.json`, является главным переключателем.
- **`src/lib/env.ts`**: Функция `useSqliteDev()` читает эту переменную.
- **`src/lib/db/index.ts`**: Главная функция `query()` проверяет результат `useSqliteDev()` и направляет SQL-запрос либо в адаптер для SQLite, либо в адаптер для PostgreSQL.
- **Адаптер SQLite**: Слой совместимости в `src/lib/db/adapters/sqlite-adapter.ts` "на лету" переводит некоторые конструкции PostgreSQL (например, `RETURNING`, `uuid_generate_v4()`) в синтаксис, понятный SQLite.
- **`db/schema-portable.sql`**: Здесь живет SQL-схема, специально адаптированная для работы и с PostgreSQL, и с SQLite.
