# 📝 Рецепт: Локальная разработка с SQLite

Этот документ описывает, как настроить и использовать локальную базу данных SQLite для разработки и тестирования.

---

## 🎯 Цель

Использование SQLite в качестве локальной базы данных позволяет:

- **Быстро стартовать:** Настройка окружения сводится к одной команде.
- **Работать безопасно:** Вы полностью изолированы от "боевой" (production) базы данных, что исключает риск случайной порчи реальных данных.
- **Разрабатывать оффлайн:** Не требуется подключение к облачной БД.
- **Иметь консистентные данные:** У каждого разработчика есть одинаковый набор начальных данных для тестирования.

---

## 🚀 Быстрый старт

### 1. Первичная инициализация БД

В первый раз (или если вы хотите начать с чистого листа), выполните команду:

```bash
npm run db:init:sqlite
```

**Что она делает?**

1.  Проверяет наличие файла схемы `db/schema-portable.sql`.
2.  **Удаляет** старый файл `dev.sqlite`, если он существует.
3.  Создает новый файл базы данных `dev.sqlite`.
4.  Применяет к нему всю необходимую структуру таблиц (схему).
5.  **Наполняет (seeds)** базу начальными данными (категории, тестовые товары), чтобы приложение сразу было работоспособным.

### 2. Запуск сервера разработки

Для повседневной разработки используйте команду:

```bash
npm run dev
```

Эта команда автоматически запускает приложение в режиме разработки с подключением к вашей локальной базе `dev.sqlite`.

---

## ⚠️ Важное ограничение среды

**Внимание:** В среде разработки Firebase Genkit Studio **отсутствует** консольная утилита `sqlite3` и возможность ее установки. Это означает, что вы не можете напрямую выполнять SQL-запросы к файлу `dev.sqlite` из командной строки терминала.

Взаимодействие с базой данных должно осуществляться **только через код**, используя библиотеку `better-sqlite3`, как это сделано в нашем сервисном слое (`src/lib/db.ts`).

Все скрипты в `package.json`, которые пытались использовать `sqlite3` CLI, были удалены.

---

## 🛠️ Полезные команды

### Сброс базы данных до начального состояния

Если ваши локальные данные "замусорились" или вы хотите вернуться к исходному состоянию, просто выполните:

```bash
npm run db:reset-sqlite
```

Эта команда удаляет файл `dev.sqlite` и связанные с ним временные файлы. После этого для пересоздания базы запустите `npm run db:init:sqlite`.

### Запуск с PostgreSQL

В редких случаях, когда вам нужно протестировать что-то в окружении, максимально приближенном к production (с PostgreSQL), вы можете использовать команду:

```bash
npm run dev:postgres
```

**Важно:** Для этого у вас должны быть настроены переменные окружения для подключения к PostgreSQL в файле `.env.local`.

---

## ⚙️ Как это работает (для любопытных)

- **`USE_SQLITE_DEV=true`**: Эта переменная окружения, установленная в скриптах `package.json`, является главным переключателем.
- **`src/lib/env.ts`**: Функция `useSqliteDev()` читает эту переменную.
- **`src/lib/db.ts`**: Главная функция `query()` проверяет результат `useSqliteDev()` и направляет SQL-запрос либо в адаптер для SQLite, либо в драйвер для PostgreSQL.
- **Адаптер SQLite**: Слой совместимости в `db.ts` "на лету" переводит некоторые конструкции PostgreSQL (например, `RETURNING`, `uuid_generate_v4()`) в синтаксис, понятный SQLite.
- **`db/schema-portable.sql`**: Здесь живет SQL-схема, специально адаптированная для работы и с PostgreSQL, и с SQLite.
